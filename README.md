# patterns-tc-benchtop

The package-set (pattern) for **Technicomp Benchtop Linux**, an immutable
Tumbleweed-based openSUSE derivative. Built on OBS directly from this repo via
scmsync. The full merge rationale and the not-yet-included package inventory
live in the `benchtop-linux` repo.
